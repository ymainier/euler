import { describe, it } from "vitest";
import { reflect } from "./reflector.ts";

describe("reflect", () => {
  it.todo("parses a 'done' decision correctly");
  it.todo("parses a 'replan' decision correctly");
  it.todo("parses an 'escalate' decision correctly");
  it.todo("extracts token usage from the SDK response");
  it.todo("rejects when the timeout expires");
});

void reflect;
